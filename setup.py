from setuptools import setup
"""
setup (
    author="Mostafa Mohamed Atef",
    description="Package for executing files, programs, websites with shortcuts",
    name="aukey",
    version="0.1.0",
    packages=find_packages(include=["aukey", "aukey.*"]),
    install_requires=['platform','subprocess','typing','json','keyboard'],
)
"""